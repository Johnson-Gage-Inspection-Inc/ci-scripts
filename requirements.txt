# Production dependencies
openpyxl>=3.1.0

# Development and testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
# pin black to a major release to ensure CI and local versions behave consistently
# (black 25.x only supports Python 3.9+; older matrix entries were removed)
black>=25.0.0,<27.0.0
flake8>=6.0.0
mypy>=1.0.0
requests>=2.28.0
